#include "main.h"
#include <stdio.h>
#include <time.h>
#include <stdlib.h>
/**
 * main - check if correct for correct output
 *
 * Return: always 0(success)
 */
int main(void)
{
	int n = 0;

	positive_or_negative(n);
	return (0);
}
